===RubyClock===

A simple binary clock written in ruby that continuously updates and displays the time on the command line/terminal in both binary and digital read-out.

To run the clock simply run the binaryclock.rby file in your Windows console.